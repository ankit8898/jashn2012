<!DOCTYPE html>
<html>
<head>
	<title>Jashn 2012</title>
	<%= favicon_link_tag 'favicon.ico' %>
	<%= stylesheet_link_tag    "application", :media => "all" %>
	<%= javascript_include_tag "application" %>
	<%= csrf_meta_tags %>
</head>
<body>
	<div class="container">
		<div class="row">
			<div class="span2">
			</div>
			<div class="span8">
				<%= yield %>
				<%= render 'shared/footer' %>
			</div>
		</div>

	</div>

</body>
</html>
