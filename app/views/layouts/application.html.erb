<!DOCTYPE html>
<html>
<head>
	<title>Jash2012</title>
	<%= stylesheet_link_tag    "application", :media => "all" %>
	<%= javascript_include_tag "application" %>
	<%= csrf_meta_tags %>
</head>
<body>
	<div class="container">
		<%= render 'shared/top_carousel' %>
		<div class="row">
			<div class="span4">	
				<%= yield %>
			</div>
			<%= render 'shared/right_panel' %>
		</div>

	</div>
</body>
</html>
