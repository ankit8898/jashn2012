<!DOCTYPE html>
<html>
<head>
	<title>Jashn 2012</title>
	<%= favicon_link_tag 'favicon.ico' %>
	<%= stylesheet_link_tag    "application", :media => "all" %>
	<%= javascript_include_tag "application" %>
	<%= csrf_meta_tags %>
</head>
<body>
	<div class="container">
		<%= render 'shared/top_carousel' %>
		<div class="row">
			<div class="span4">	
				<%= yield %>
			</div>
			<%= render 'shared/right_panel', :guests => @guests %>
	</div>
	
	
</body>
</html>
